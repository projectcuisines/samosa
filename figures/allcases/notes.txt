Generated by Claude:

Summary Figures:

  1. Water vapor figure: The log-space kriging masks out the runaway cases for ExoCAM and ROCKE-3D, matching the treatment in the temperature and cloud fraction figures and in fig_spread.py.
   (Corrected 2026-08-13: the kriging calls had been passing the full arrays, so the 1e4 kg/m^2 sentinel was being fit as if it were data while the panel titles advertised the masked count. The
   ExoCAM panel shifted by a median of 1.78 dex when this was fixed. No numbers quoted in the manuscript changed, because the water vapor Results text cites raw sample values and the spread
   figure was already masking correctly.)
  2. Water vapor figure: PlaHab panel shows no data because it is a 2D model and does not simulate water vapor.                                                                                                     
  3. Cloud fraction figure: Total cloud fraction is the column-integrated union of cloud at all vertical levels, which Eric noted makes it a difficult quantity to interpret and interpolate. The broader hatching  
  reflects this.                                                                                                                                                                                                    
  4. All interpolation figures: Hatched regions indicate where the kriging standard deviation exceeds the threshold (sigma_threshold), signaling that the interpolation is unreliable there.                        
  5. All interpolation figures: The linear variogram model was selected based on leave-one-out cross-validation across all models and variables.                                                                    
  6. All interpolation figures: Coordinates were normalized to [0,1] (log-pressure and flux) before kriging to balance the distance metric.


Energy Balance Figure (fig_energy_balance.py, added 2026-08-13):

  Plots (OLR - ASR)/(S/4) x 100 %, i.e. the residual TOA imbalance as a percentage of the incident stellar flux. S/4 is fixed by the protocol, so the
  denominator is identical across models and the comparison is exact. Positive = still cooling. Symlog y-axis, linear inside +/-1%.

  Sources: ExoCAM output.txt (OLR, toaEBAL; ASR = OLR + toaEBAL); ExoPlaSim samosaNN.nc area-weighted rst/rlut (note rsut and rlut are stored negative);
  ROCKE-3D rocke_NNq.nc -trnf_toa_hemis[2]/srnf_toa_hemis[2]; Generic PCM and LFRic from their .dat/.txt global output; PlaHab from
  simulations/sampleN/ (NOT the top-level global_samosa_plahab_seq1sam4.dat, which is a stale duplicate disagreeing by ~3 K in Tglob).

  Result: only the Generic PCM has no accepted case within 1% (median 3.3%, max 19.7% at Case 10). Everything else is mostly inside 1%. The four
  ROCKE-3D cases above 3% (2, 6, 10, 12) are exactly the four flagged in the ROCKE-3D README as unstable or as retaining an imbalance.

  Caveat: TOA balance is necessary but not sufficient for equilibrium -- a run can be balanced at TOA while the slab ocean still drifts.

  ExoCAM archive gap: in the submitted samosaN.cam.h0.avg.nc files, FSNTOA, FSNTOAC, FSNTC, FSDSC, FLUTC, SWCF and LWCF are ALL identically zero.
  Of the BROADBAND fields only FSNT, FSDS, FLUT and FLNT carry data. DECISION (2026-08-15): use only the populated fields and derive ExoCAM from the
  NetCDF like every other model, rather than mixing a text summary with primary data. FSNT is top-of-model, so albedo came out ~0.3 pp above TOAALB
  (0.9 pp at Case 12) and the imbalance up to ~1.3 W/m^2 from toaEBAL (cases 11 and 12 differ most: 1.35% vs 0.70%, 0.97% vs 0.20%). Use gw (gauss
  weights, in the files) NOT cos(lat) -- gw-weighted TS reproduces the summary TS exactly for all 11 files, which is the check that validates the
  whole extraction.

  Anisotropy cap fix (2026-09-17): fit_anisotropy.py computed the tolerance pick (smallest scaling within 2% of the best LOO, "a smaller ratio
  distorts less") and then discarded it whenever the LOO minimum sat at the top of the search range, snapping to CAP=15 instead. That chose the most
  distorting ratio of a set the data cannot tell apart -- the opposite of the rule everywhere else. The ceiling now applies TO the tolerance pick:
  pick = min(tolerance pick, CAP). Three models hit that branch: LFRic temperature stays 15 (its LOO falls monotonically and the tolerance pick is 15
  anyway), Generic PCM water vapor goes 15 -> 10, ExoCAM albedo goes 15 -> 7. Neither changes a panel visibly: max field difference is 0.33 against a
  data sd of 2.19 for the PCM and 0.043 against 0.32 for ExoCAM. No manuscript number moves -- only the temperature and cloud-fraction ratios are
  quoted in the text, and neither changed.

  ExoCAM albedo variogram family, 2026-09-17 (SUPERSEDES the paragraph below, which concluded the flatness was irreducible -- it was not).
  The lower-block ExoCAM panel fitted a pure nugget on the seven common cases and was stippled, while the upper block resolved a surface. Jacob was
  right that the two cannot both be true of the same model and variable. The anisotropy is NOT the culprit (swept 1-50, resolution never exceeds 0.064
  against the 0.25 floor, best at isotropic, while every other model in that block reaches 0.58-0.96) -- the VARIOGRAM FAMILY is. LOO prefers
  spherical for ExoCAM albedo on BOTH sets independently: 0.288 vs 0.341 (full) and 0.278 vs 0.325 (common). Refitting the anisotropy under spherical
  gives 15, an interior optimum rather than a capped one, with LOO 0.258 on the full set (24% better than linear) and resolution 0.60 / 0.88 in the
  two blocks. Both fits stay strictly inside the sample range (no overshoot) and sigma stays well under the hatch threshold. fig_interpolation_albedo.py
  now carries a VARIOGRAM dict keyed by model, and fit_anisotropy.py a FAMILY dict keyed by (variable, model), so a rerun after the next resubmission
  reproduces 15 instead of quietly handing back the linear answer of 7.

  Also tried and rejected: universal kriging with a regional_linear drift (LOO 0.49, far worse than any OK variant); power (LOO 0.319 and barely
  resolves at 0.07; note power gives resolution 2.9-3.3 for Generic PCM and ExoColumn, i.e. it swings well outside the data range -- the resolution
  guard needs an UPPER bound before power is ever adopted); borrowing the full-set linear variogram parameters for the common block (works, LOO 0.300,
  resolution 0.28, but it is a patch where a better-fitting family exists). Gaussian is a close second to spherical (0.281 common, 0.322 full).

  General lesson: a degenerate ordinary-kriging fit means "wrong model", not "insufficient data". Check the variogram family before concluding that a
  panel cannot resolve -- crossval_variogram_*.py already exists to answer exactly this, and the figure scripts had been hard-coding linear past it.

  ExoCAM albedo panel, on the low contrast of it: this is the DATA, not the fit. The kriged field's resolution (std of the surface over std of the
  samples) is 0.40 at the fitted ratio against a 0.25 eligibility floor, and peaks at only 0.414 anywhere in the search range, so no scaling makes
  that panel more structured. It looks flat because ExoCAM's albedo spans 16.5-34.2% on a 0-45% scale. The raw variogram slope (0.012) is misleading
  here -- it is in logit units under an anisotropic metric, which is exactly why fit_anisotropy.py has the resolution metric instead.

  ALBEDO PART RESOLVED 2026-09-17. The gap was never as wide as this note assumed: the RESOLVED flux profiles FDS and FUS (shortwave down and up on
  ilev) are fully populated, and ilev[0] is the top of the model. gw-weighted FUS[0]/FDS[0] is the planetary albedo and reproduces the summary TOAALB
  to <=0.03 pp in all ten rows of output.txt -- so the albedo needed none of the zeroed fields, and we get it from primary output AND agree with the
  group's own number at the same time. extract_fluxes.py now derives it this way and asserts the TOAALB agreement as a second validation alongside
  the TS check. Why the old route was biased: mean FDS[0], the insolation the model actually sees, runs 0.4-0.5% below the protocol S/4 (124.43 vs
  125.00 at Case 1), and 1 - FSNT/(S/4) inherits that deficit as an albedo 0.3-0.9 pp too high. The imbalance still uses FSNT, so nothing in Figure 2
  changes; SWCF/LWCF and the clear-sky fields are still missing, so the cloud-radiative-forcing ask to Eric stands.

  Consequences: albedo spread max 41.3 -> 41.7 pp (the maximum moves from Case 10 to Case 11), median unchanged at 19.9, and the fitted anisotropy is
  unchanged for every model. The ExoCAM albedo variogram still fits slope=0 exactly on the seven common cases, so that panel stays stippled.
  Lesson: check the profile variables before declaring a derived quantity unrecoverable from a broadband one.

  ExoCAM Case 7: exocam/samosa7.cam.h0.avg.nc holds a real run (TS=345.1 K, ICEFRAC=0, ASR=309.3, OLR=288.4 -> -5.23%) but has no row in output.txt.
  It is a runaway in progress, so excluding it from the interpolated figures is right, but it belongs on the energy balance figure as an open marker.
  Deriving ExoCAM from the NetCDF picks it up automatically.

  Still worth asking Eric for a resubmission with the TOA and clear-sky fields populated -- it would let ExoCAM be verified like the others and would
  make cloud radiative forcing (SWCF/LWCF) available to the intercomparison.

  LFRic verification: lfric_samosa_caseNN.nc sw_net_toa and lw_up_toa reproduce the .txt global diagnostics exactly (ASR, OLR, albedo, imbalance all
  to the last digit), so the LFRic numbers used here are confirmed against primary data.

  LFRic Case 7 (submitted 2026-09-04, ACCEPTED after query): the file is self-consistent -- lfric_samosa_case07.nc reproduces every column of the
  new samosa_global_diagnostics_lfric_2026-09-04.txt row to 2 dp (Tglob 400.52, Tmax 427.12, Tmin 389.82, cldfrac 0.34, Qmass 1735.22, OLR 502.62,
  ASR 386.24), so this is not an extraction or a mapping error. It is nonetheless extreme on every diagnostic:
    - TOA imbalance +29.09%, against -0.37 to +0.98% across every other LFRic case, i.e. 30x the next largest, and the largest value anywhere
      in the ensemble (the next is Generic PCM Case 10 at +19.66%).
    - Planetary albedo 3.4%, against 21-36% elsewhere, despite a reported cloud fraction of 0.34.
    - Stratospheric specific humidity 3.04e-01 kg/kg, a saturated steam stratosphere and the extreme end of the wet-stratosphere bias already
      logged for Case 16. (Originally written here as "five orders of magnitude above the <=1.8e-06 of every other case"; that was wrong even then,
      since Cases 12 and 16 carry 1.23e-04 and 1.54e-05. It was 3.4 dex above Case 12, the next wettest; since 2026-09-10 the next wettest is
      Case 11 at 5.44e-04, 2.7 dex below it.)
  OLR of 502.6 W/m^2 over a 1735 kg/m^2 water column is also far above the ~276-288 W/m^2 at which LFRic's own Cases 12 and 16 settle, which is roughly
  where the Simpson-Nakajima limit should cap it. Its 400.52 K global mean is 107.5 K above the warmest other model at that sample point (PlaHab 293.0;
  ExoPlaSim 279.7; ROCKE-3D 267.7; ExoCAM runaway).
  All of that reads like a run still cooling from a hot start, so it was queried rather than assumed. Sergeev (2026-09-04): the imbalance is not
  improving, i.e. it is persistent rather than a decaying spin-up transient, and he attributes it to a cloud layer becoming stuck at the very top of
  the model domain. DECISION (2026-09-04, with Jacob): if that is the model's behavior we use it. Case 7 is therefore accepted on the same footing as
  every other submitted case, and LFRic goes from 7 to 8 cases across the analysis.
  What moved as a result:
    - fig_energy_balance.py: accepted['LFRic'][6] True. Case 7 is now the largest imbalance plotted anywhere in the figure.
    - Anisotropy refit (fit_anisotropy.py): LFRic cloud fraction 15 -> 1, LFRic albedo 1 -> 1.5. Temperature (15) and water vapor (15) unchanged,
      and no other model moved.
    - Kriging skill drops sharply for LFRic temperature: leave-one-out RMSE at the fitted ratio of 15 goes 24.64 -> 37.83 K. 400.52 K at
      (1600, 0.55) is not predictable from the other seven cases, so the LFRic panel of the temperature figure is now the least constrained.
      Isotropic LOO for the same field goes 30.64 -> 54.13 K, and LFRic alone now prefers a power variogram (43.04) over linear; the ensemble mean
      still favors linear (31.79 vs 32.73), so the uniform linear choice stands.
    - fig_summary.py: "all models above freezing" 52.6% -> 52.5%. Below-freezing (32.3%) and contested (15.2%) unchanged.
    - All nine allcases PNGs change. LFRic panel labels are computed from array length, so they self-update to n=8.
  Still open with Sergeev: get the stuck-cloud-layer attribution in writing before it goes in print, and bundle with the surface-flux and
  wet-stratosphere asks. If the cloud layer is later unstuck and the case resubmitted, this all has to be redone.

  LFRic Cases 8, 10, 11 (submitted 2026-09-10, ACCEPTED): samosa_global_diagnostics_lfric_2026-09-10.txt adds exactly these three rows; every
  earlier row is byte-identical. Per Jacob this completes the SAMOSA submissions. LFRic now has 11 cases; its five gaps (2, 3, 5, 6, 13) are
  ExoCAM's runaway set less Case 7, but the group has not said whether they are runaways.
  Validation: same variable set as Case 16 in all three files, and cos(lat)-weighted fields reproduce every column of the new rows (Tglob, Tmax,
  Tmin, OLR, ASR, Fsdn, Fnet, Qmass, Cldliq, Cldice, Cldfrac) to the printed precision. Sanity checks against the ensemble:
    - Tglob 231.83 / 197.81 / 227.52 K sits inside the other models' range at each point (215.9-248.5, 172.8-210.7, 181.1-242.6).
    - Albedo 33.84 / 23.99 / 30.39%, inside LFRic's own 21-36%.
    - TOA imbalance -1.28 / -1.65 / +2.12%: the first LFRic cases other than Case 7 above the 1% rule, but the same band as ExoCAM Cases 1 and 10
      and PlaHab Cases 8 and 14, and worth at most ~2 K of drift on a Planck-only estimate. Accepted without query.
    - Case 11 Qstrat 5.44e-04 kg/kg is the model-top value, and at 0.10 bar the model top is only 1.36 hPa above a 98 hPa surface, so it is
      geometry rather than another wet-stratosphere case. Cases 8 and 10 are very dry aloft (5.2e-09, 2.3e-09).
    - Case 10 Qmass is printed as 0.04 in the .txt; the NetCDF gives 0.036. The .txt value is used, as for every other LFRic case.
  What moved as a result:
    - Anisotropy refit: LFRic water vapor 15 -> 10, LFRic albedo 1.5 -> 3. No other model moved.
    - LFRic temperature LOO at the fitted ratio is 32.34 K with all 11 cases. Without Case 7 the ten remaining cases fit a ratio of 4 at 21.27 K,
      so Case 7 is now what pushes LFRic's temperature anisotropy to the cap of 15 (24.64 -> 37.83 K above was the 7 -> 8 case version).
    - LFRic albedo now resolves even with Case 7 withheld, though only at ratio 3, so the panel no longer rests on that one point alone.
    - Isotropic variogram-family LOO: LFRic no longer prefers power for temperature (linear 53.94 vs power 54.58). But the ensemble mean for
      cloud fraction now favors spherical over linear (0.799 vs 0.847, driven by LFRic 1.130 -> 0.949), and water vapor marginally favors power
      (2.052 vs 2.064). Linear is still used uniformly; flagged, not changed.
    - fig_summary.py: area fractions unchanged (32.3 / 52.5 / 15.2%); 12 of 44421 display cells move between warm and contested. The LFRic
      isotherm segment lengthens because its hull now reaches 0.10 bar.
    - fig_spread.py: T median 19.8 -> 19.7 K, min 7.1 -> 6.7 K; cloud maximum moves from (400, 4.83) at 32.6 to (900, 0.10) at 31.1, i.e. onto
      LFRic Case 11 (87%, the highest cloud fraction at that point).
    - Regime diagnostics: Rhines criterion 42 of 57; jet state unanimous at 5 of 11 multi-model cases (Case 10 joins the disagreements, LFRic SJ
      against four DJ); ROCKE-3D, not ExoPlaSim, is windiest at Cases 8 and 10, which are now five-model cases; LFRic Case 11 has a 48.7 m/s
      equatorial jet, at least 19 m/s above the other three models there and far above every other single-jet case (<= 31.2).
    - Pre-existing, caught by the fig_summary cross-check: fig_energy_balance.py had Case 9 as 'frozen' although HEXTOR puts it at 277.2 K.
      Stale since HEXTOR joined on 2026-09-01; now 'mixed'.
    - The direct terminator calculation of night-side convergence was NOT repeated for the new cases: its reference-energy correction was never
      saved, and neither a contour-mean nor a per-column reference reproduces the documented failure list. The raw calculation puts LFRic Cases
      8 and 10 at the wrong sign with a spurious mass flux of 6.7e11 and 5.0e11 kg/s, like ExoCAM 8 and 10.


Albedo Figure (fig_interpolation_albedo.py, added 2026-08-13):

  Same 6-panel kriging layout as the other interpolation figures, logit-space like cloud fraction, runaways excluded. Albedo taken as reported where
  available (ROCKE-3D plan_alb), as reflected over incident where the model reports both (ExoPlaSim rsut/(rst+rsut), ExoCAM FUS[0]/FDS[0]), and
  otherwise 1 - ASR/(S/4); the routes agree to <0.01 pp for ROCKE-3D Case 1 and <=0.03 pp for ExoCAM against its summary TOAALB.

  Color scale 0-45% (extend='both'), changed 2026-09-17 from 10-45%. The old floor put four of HEXTOR's nine cases and LFRic's Case 7 into the
  under-color, so those two panels read as dark slabs with no structure. Colormap changed at the same time from cmocean.ice, which bottoms out at
  near-black (L* 1.8), to cmocean.haline, which holds a readable lightness across the whole 0-45% range. PlaHab Case 11 (63.7%) ran past the top until
  PlaHab resubmitted it at the protocol 900 W/m2 on 2026-09-22 (the first run was at 400 W/m2); it is now 23.8%, and every sample
  lies inside the range.

  sigma_threshold=1.0 logit units now trips for PlaHab, LFRic and HEXTOR, in each case almost entirely at instellations above the warmest case that
  model ran. PlaHab's variogram is inflated by the extreme point at Case 10 (0.9%; the other, 63.7% at Case 11, went with the 2026-09-22
  resubmission); the Case 10 value looks wrong for a 190 K
  surface and that case also carries a -9.7% energy imbalance.

  Result: albedo spread (median 19.9 pp) is about half the cloud fraction spread (median 41.9 pp), but the model ranking is still unstable --
  ExoPlaSim is brightest at 6 sample points and darkest at 4.

  DEGENERATE PANELS: the linear variogram fits slope=0 exactly for ExoCAM and LFRic, i.e. a pure nugget. Ordinary kriging then weights all points
  equally regardless of distance and returns the sample mean everywhere, so those panels are flat and carry no spatial information; only the
  exact_values=True pinning at the sample points survives. They are now stippled and labelled in the figure. This was NOT caught by reusing the
  linear variogram from the other interpolation figures -- crossval_variogram_albedo.py was written afterwards to check it.

  crossval_variogram_albedo.py: linear still wins on mean LOO RMSE (0.461 vs 0.468 exponential, 0.479 spherical, 0.482 power, 0.484 gaussian), so it
  remains the right global choice. Per model: for ExoCAM every non-degenerate alternative is worse (0.374-0.401 vs 0.360), so a constant really is the
  best estimate there. For LFRic (only 6 points) exponential does better (0.297 vs 0.334), so its flatness is partly a fitting artifact.

  Related pre-existing issue: in fig_interpolation_clouds.py the Generic PCM fit has slope=0.0079, i.e. very nearly degenerate too (field ptp 0.89
  logit vs 2.3-4.8 for the other models). That panel of the cloud fraction figure is close to flat for the same reason and is worth revisiting.


Standard Deviation Figure:

  Spread metric                                                                                                                                                                                                     
  Each panel shows the variance-weighted inter-model standard deviation across the full (instellation, pN₂) parameter space. For surface temperature the std is in K; for water vapor it is in dex (σ of log₁₀ of   
  the column in kg m⁻²); for cloud fraction it is in %.                                                                                                                                                             
                                                            
  Kriging                                                                                                                                                                                                           
  Each model's data were independently interpolated onto the common grid using Ordinary Kriging with a linear variogram, with axes normalized to [0,1] before fitting. Water vapor was kriged in natural-log space;
  cloud fraction in logit space (then back-transformed via sigmoid to % before computing the spread).                                                                                                               
                                                            
  Runaway exclusion                                                                                                                                                                                                 
  Cases flagged as runaway greenhouse (sentinel values: 600 K for temperature, 10⁴ kg m⁻² for water vapor, 200% for cloud fraction) were excluded from kriging for all variables. PlaHab provides no water vapor
  data and is excluded from that panel only.                                                                                                                                                                        
  
  Variance weighting                                                                                                                                                                                                
  Rather than a simple standard deviation, each model's contribution is weighted by the inverse of its kriging variance (w_i = 1/σ²_i), so models that are interpolating near actual sample points carry more weight
   than those extrapolating into unsampled regions. To prevent sample points from receiving arbitrarily large weight (kriging variance is exactly zero at exact data locations with exact_values=True), the variance
   floor is capped at 10% of the median kriging variance across all models and grid points, bounding the maximum weight ratio at approximately 10:1.
                                                                                                                                                                                                                    
  Single-model masking                                      
  Hatched regions indicate areas where only one model (ExoPlaSim) has valid sample data — specifically the three QMC sample points at (1900 W m⁻², 7.85 bar), (2400 W m⁻², 0.21 bar), and (2100 W m⁻², 1.83 bar),
  which are runaway cases for ExoCAM, ROCKE-3D, and PlaHab and outside the sampling subsets of Generic PCM and LFRic. The boundary was determined by kriging the per-point model count and masking where the        
  interpolated count falls below 1.5. Sample points are shown as open circles (multiple models) or × marks (single model only).


Caption trim (2026-09-21):

  The figure captions in main.tex were cut to what is plotted and how to read it, from 32.6k to 11.1k characters in
  total. Method, results and caveats moved into the adjacent bracketed body paragraphs, and TO DOs into those
  paragraphs' TO DO lists. The details below were dropped from the paper outright and are kept here instead.

  Figure 2: the regime shading assigns seven cases to the frozen regime, two to the ice-free regime, four to the
    contested band and three to the runaway region.
  Figures 3 and 5: the unhatched slivers beyond the outermost samples exceed the sigma threshold by at most 12 K
    (temperature) and 0.24 logit units (cloud fraction).
  Figure 4: the lower block's color scale runs down to 1e-4 kg m^-2 to include ExoColumn at Case 10.
  Figure 6: the upper color floor was 10% until 2026-09-17, which put four of HEXTOR's nine cases and LFRic Case 7
    in the under-color and left both panels reading as featureless dark blocks. The lower block reaches the same
    0% floor because PlaHab gives 0.9% at Case 10 and HEXTOR 3.4 and 3.3% at Cases 4 and 9.
  Figure 13: HEXTOR's belts come from zonal_output_HEXTOR_caseNN.dat. The uniform-longitude average recovers the
    submitted global mean to within 0.03 K for ROCKE-3D, LFRic and PlaHab and 0.39 K for the other resolved models
    (the rounding of the reported values), and 0.01 K for the mapped HEXTOR curves.
  Figure 16: colors identify the model rather than the regime, as they do in the source figure, because the regime
    is already carried by the marker.
  Figure 17: adding a model can only shrink the two consensus regions, so the white band is a subset of the full
    contested band and the grey is exactly the area that PlaHab alone makes contested.
